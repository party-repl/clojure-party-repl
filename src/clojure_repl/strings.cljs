(ns clojure-repl.strings
  "Collection of all the UI strings and messages to be shown to the end user.")

(def remote-repl-header "Connect to remote nREPL server")
(def remote-repl-host "Host")
(def remote-repl-port "Port")
